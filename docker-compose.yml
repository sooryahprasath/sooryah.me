services:
  web-nginx:
    build:
      context: .
      # Use host network for DNS resolution during npm install
      network: host 
    container_name: web-nginx
    restart: unless-stopped
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - web-network

  web-tunnel:
    image: cloudflare/cloudflared:latest
    container_name: web-tunnel
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    env_file: .env
    networks:
      - web-network

networks:
  web-network:
    driver: bridge